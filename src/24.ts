import { createTheme } from '@material-ui/core';

const theme = createTheme({
  palette: {
    primary: {
      main: '#2e7d3a',
    },
    secondary: {
      main: '#1f8ad5',
    },
    error: {
      main: '#ff4961',
    },
  },
});

export default theme;
